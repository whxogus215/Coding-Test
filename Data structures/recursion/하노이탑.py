def TOH(int n, int a, int b, int c): # a에서 c로 옮기는데 b를 사용
        """
        0. a에서 c로 원반을 옮기는 작업.
        1. a에서 마지막 원판을 제외하고 나머지를 모두 b로 옮긴다. (c를 사용)
        2. a에 남은 가장 큰 원판을 c에 옮긴다.
        3. b에 남은 n-1 개의 원판을 모두 c로 옮긴다. (a를 사용)
        4. 모든 원판이 a에서 c로 옮겨졌다. 
        """
    if (n>0):
        TOH(n-1,)


        